import React from "react";

const AllOrders = () => {
  // const { data, isLoading } = useQuery("orders");
  return (
    <div>
      <h1>All orders</h1>
    </div>
  );
};

export default AllOrders;
